# Copy this file to `.env` and fill in values

# Supabase project settings → Project URL and anon public key
VITE_SUPABASE_URL=https://YOUR-PROJECT-REF.supabase.co
VITE_SUPABASE_ANON_KEY=YOUR-ANON-PUBLIC-KEY

# Optional: WordPress site URL for syncing and/or direct fetch
VITE_WP_API_URL=https://your-wordpress-site.com

# Server-side only: Supabase service key for sync script (do NOT expose client-side)
# Use a local .env only; never commit real keys.
SUPABASE_SERVICE_KEY=your-service-role-key
